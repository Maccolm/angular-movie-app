import { ExtractYearFromDatePipe } from './extract-year-from-date.pipe';

describe('ExtractYearFromDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractYearFromDatePipe();
    expect(pipe).toBeTruthy();
  });
});
