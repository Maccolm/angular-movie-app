import { LimitedSymbolsPipe } from './limited-symbols.pipe';

describe('LimitedSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitedSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
