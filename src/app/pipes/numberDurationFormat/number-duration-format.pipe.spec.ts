import { NumberDurationFormatPipe } from './number-duration-format.pipe';

describe('NumberDurationFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberDurationFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
